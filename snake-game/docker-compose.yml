version: '3'

services:
  
  server: 
    build: ./server
    expose:
     - 3001  
    ports:  
     - 3001:3001
    # volumes:
    #  - ./server:server
    command: npm start

  client: 
   build: ./client
   expose:
     - 3000  
   ports:  
     - 3000:3000
  #  volumes:
  #    - ./client:client

   links:
     - server
   command: npm start
  